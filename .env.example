# The LLM to handle the main chat requests
MODEL=your-main-chat-model-name

# The LLM to run N parallel iterations
ITER_MODEL=your-iterative-model-name

# The LLM to judge/compare the N iterations
JUDGER_MODEL=your-judger-model-name

# Number of parallel iterations to run (integer)
NUM_ITERS=3
